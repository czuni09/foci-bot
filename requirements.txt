# Core runtime dependencies (pinned for reproducible builds)

# aiohttp and related low-level packages pinned for compatibility
aiohttp==3.9.4
yarl==1.9.2
multidict==6.0.4
frozenlist==1.3.3

# HTTP + scraping / parsing / data packages
requests>=2.31.0
understat>=1.0.0
feedparser==6.0.10

# Data science / plotting
pandas==2.1.4
numpy==1.26.5
plotly==5.18.0
python-dateutil==2.8.2

# Database / ORM
SQLAlchemy==2.0.23

# Add any other dependencies your app needs below
